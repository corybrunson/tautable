system.time(5==5)
system.time(5==5)->tets
names(tets)
tets$elapsed
tets[[1]]
tets[[2]]
tets[['elapsed']]
tets$elapsed
require(tautable)
N <- seq(50, 200, 50)
st.mat <- sapply(N, function(n) {
system.time(tau.crit.tab(n = n,
alpha = example.alpha,
incl.len = TRUE))
})
example.n <- seq(10, 100, 10); example.alpha <- 10 ^ (-1:-4)
st.mat <- sapply(N, function(n) {
system.time(tau.crit.tab(n = n,
alpha = example.alpha,
incl.len = TRUE))
})
N <- seq(50, 100, 50)
st.mat <- sapply(N, function(n) {
system.time(tau.crit.tab(n = n,
alpha = 10^(-1:-3),
incl.len = TRUE))
})
N
N <- seq(50, 50, 25)
st.mat <- sapply(N, function(n) {
system.time(tau.crit.tab(n = n,
alpha = 10^(-1:-3),
incl.len = TRUE))
})
tau.crit.tab
tau.crit.table
mahonians.recursive
N <- seq(25, 100, 25)
st.mat <- sapply(N, function(n) {
system.time(tau.crit.table(n = n,
alpha = 10^(-1:-3),
incl.len = TRUE))
})
st.mat
st.mat[3, ]
plot(x = N, y = st.mat[3, ], type = 'o',
xlab = 'Maximum number of objects', y = 'Time elapsed')
N
